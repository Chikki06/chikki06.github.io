# Dependencies
node_modules/

# Build output (generated by `npm run build`; deploy uses gh-pages -d dist)
dist/

# OS
.DS_Store
Thumbs.db

# Logs and debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env (do not commit secrets)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Editor / IDE (optional: remove if you want to commit shared settings)
.idea/
.vscode/
*.swp
*.swo
*~
prompt.md

# Keep these in repo: public/, content/, src/, index.html, package.json, vite.config.js, etc.
# Asset endpoints (must live under public/): /favicon.svg, /assets/, /pdfs/, /timeline/, /images/
